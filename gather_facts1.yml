---
- name: Issue show commands
  hosts: all
  connection: network_cli
  tasks:
    - name: Gather all legacy facts
      cisco.ios.ios_facts:
        gather_subset: all
        
        
    
        
