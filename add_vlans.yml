---
- hosts: all
  gather_facts: false
  connection: network_cli 
  tasks:
    - name: Create new VLAN 
      cisco.ios.ios_vlans:
        config:
        - name: VLAN_10
          vlan_id: 10
          state: active
      
    - name: save running to startup when modified
      ios_config:
        save_when: modified
